<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>
    <style name="ThemeCustomDialog" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="dialogCornerRadius">20dp</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

    </style>

<!--    <style name="mySwitch" parent="Theme.AppCompat.Light">-->
<!--        &lt;!&ndash; active thumb & track color (30% transparency) &ndash;&gt;-->
<!--        <item name="colorControlActivated">#e6e600</item>-->
<!--        <item name="colorSwitchThumbNormal">#cc0000</item>-->
<!--    </style>-->


    <style name="mySwitch" parent="AppTheme.NoActionBar">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">#ffffff</item>
        <!-- inactive thumb color #f1f1f1-->
        <item name="colorSwitchThumbNormal">@color/transkripcija
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#ffffff
        </item>
    </style>

</resources>
